services:

  frontend:

    build:
      context: .
      dockerfile: docker/frontend/Dockerfile

    container_name: AppVIOL_frontend

    working_dir: /app

    volumes:
      - .:/app

    ports:
      - "5173:5173"

    stdin_open: true

    tty: true