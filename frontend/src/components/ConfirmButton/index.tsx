import { Button } from "@mui/material";

export default function ConfirmButton() {
  return (
    <Button
      fullWidth
      size="large"
      variant="contained"
    >
      Conferma consegna
    </Button>
  );
}